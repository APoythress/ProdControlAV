name: Build & Deploy API (Source → Azure Container Apps)

on:
  push:
    branches: [ "main" ]
    paths:
      - "src/ProdControlAV.API/**"
      - ".github/workflows/aca-source-deploy.yml"
  workflow_dispatch:

env:
  AZURE_REGION: eastus
  RESOURCE_GROUP: ProdControlAV-Group     # your existing RG
  CONTAINERAPP_NAME: pcav-app             # <-- set to your actual Container App name
  APP_SOURCE_PATH: src/ProdControlAV.API  # path to the API project
  TARGET_PORT: "80"                     # <-- change to 5000 if your Container App uses 5000

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Azure login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.PCAVAPP_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.PCAVAPP_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.PCAVAPP_AZURE_SUBSCRIPTION_ID }}

      # Optional: make sure we're on the right subscription context
      - name: Set subscription context
        run: az account set --subscription "${{ secrets.PCAVAPP_AZURE_SUBSCRIPTION_ID }}"

      - name: Install Container Apps extension
        run: |
          az extension add --name containerapp --upgrade || az extension update --name containerapp
          az config set extension.use_dynamic_install=yes_without_prompt

      # Build from source with Oryx/buildpacks and deploy to Container Apps
      - name: Build & Deploy to Container Apps
        uses: azure/container-apps-deploy-action@v2
        with:
          resourceGroup: ${{ env.RESOURCE_GROUP }}
          containerAppName: ${{ env.CONTAINERAPP_NAME }}
          location: ${{ env.AZURE_REGION }}
          appSourcePath: ${{ env.APP_SOURCE_PATH }}
          targetPort: ${{ env.TARGET_PORT }}
          ingress: external
          environmentVariables: |
            ASPNETCORE_URLS=http://+:${{ env.TARGET_PORT }}

      # Apply DB secret and bind EF connection string
      - name: Apply secret & env vars
        run: |
          az containerapp secret set \
            -n "${{ env.CONTAINERAPP_NAME }}" \
            -g "${{ env.RESOURCE_GROUP }}" \
            --secrets db-connstr='${{ secrets.SQL_CONNECTION_STRING }}'

          az containerapp update \
            -n "${{ env.CONTAINERAPP_NAME }}" \
            -g "${{ env.RESOURCE_GROUP }}" \
            --set-env-vars ConnectionStrings__DefaultConnection=secretref:db-connstr
